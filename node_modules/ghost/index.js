// # Ghost bootloader
// Orchestrates the loading of Ghost
// When run from command line.
console.log("ghost");
var ghost = require('./core');

function init(options){
	ghost(options).otherwise(function (err) {
    errors.logErrorAndExit(err, err.context, err.help);
	});
};

module.exports = init;
